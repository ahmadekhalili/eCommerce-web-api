import type Base from "@/helpers/i18nStructure/base";
import { exportType } from "@/helpers/exportDefaultType";
export default exportType<Base>({
  direction: "RTL",
  projectName: "فروشگاه عطر",
});
